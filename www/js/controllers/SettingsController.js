/**
 * View controller for the (modal) settings view.
 */
Songbook.controller("SettingsController", function($scope) {
    $scope.scroll = {};
    $scope.scroll.enabled = false;
    $scope.scroll.speed = 1;
});
